# How It Works

Write markdown files in the `slides/` directory, run the build command, and get beautiful Sinaptia-branded HTML presentations.

**Simple workflow:**
- Create `.md` files
- Run `rake build` or `rake server`
- Navigate with arrow keys
