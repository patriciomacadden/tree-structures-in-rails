# OTEL batch exporter

* El trace llega en el orden inverso (las hojas llegan primero)
* El batch puede no contener todo el árbol
  * 2 variables determinan la cantidad de spans que se exportan en un batch: max queue size y exporter timeout
  * Imposible encontrar la combinación perfecta para que un trace quepa en un batch
