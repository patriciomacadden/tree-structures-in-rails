# Build Commands

## Development

Run the dev server with live reloading:

```bash
rake server
```

Visit http://localhost:8000 to see your slides. Changes to markdown, templates, or assets auto-reload.

## Production

Build static HTML files:

```bash
rake build
```

Output files are generated in the `output/` directory.
